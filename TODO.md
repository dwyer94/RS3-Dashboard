Create README
Rename Clan Leaderboard, since its not actually looking at a users clan. Maybe like "Compared Players List" or something since it just uses the usernames in the tool
Hiscores — /m=hiscore/ranking endpoint is dead (returns HTML). Correct endpoint is /m=hiscore/index_lite.ws?player=NAME but returns CSV not JSON. fetchHiscores() needs a CSV parser before any hiscores widget can be built.
Skill Snapshot, no way to see which skill is which
Increase padding on the left and right of widgets by a bit
Text is all very small. Maybe give some options to increase
Give me more places to re-size widgets from
Fix encoding in News widget "Aspects &amp; Solomons Store" and ** is bold
styled scroll bars
Add a user compare to the xp heatmap?
Flesh out the DXP widget from my previous try at this app (analyzes historical market trends around DXP, tracks DXP dates, and suggests items)